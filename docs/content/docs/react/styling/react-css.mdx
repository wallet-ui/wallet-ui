---
title: React CSS
description: Using the pre-styled components from Wallet UI.
---

The `@wallet-ui/react` package includes a CSS file with a default theme for the components.

### Usage

You need to import the CSS file in your application.

```js
import '@wallet-ui/react/index.css';
```
