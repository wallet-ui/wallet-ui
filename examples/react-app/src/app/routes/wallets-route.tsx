import { PlaygroundWallet, UiStack } from '@wallet-ui/playground-react';

export default function WalletsRoute() {
    return (
        <UiStack>
            <PlaygroundWallet />
        </UiStack>
    );
}
